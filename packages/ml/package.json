{
  "name": "@agri-connect/ml",
  "version": "1.0.0",
  "description": "ML recommendation service for Agri-Connect",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "python test_service.py",
    "install": "pip install -r requirements.txt"
  },
  "keywords": [
    "machine-learning",
    "recommendations",
    "fastapi"
  ]
}
